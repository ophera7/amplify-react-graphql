type TerritorySlider @model @auth(rules: [{allow: public}]) {
  id: ID!
  Field0: String
}

type TerritorySlider2 @model @auth(rules: [{allow: public}]) {
  id: ID!
  Field0: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
} 